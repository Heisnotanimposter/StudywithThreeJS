#Light

//add ambient lighting
var ambientLight = new THREE.AmbientLight("#606060", 1);
scene.add(ambientLight);

//the point light where working with
var pointColor = "#ccffcc";
var pontLight = new THREE.PointLight(pointColor);
pointLight.decay = 0.1;
poinrLight.castShadow = ture;
scene.add(pointLight);

//add a small sphere simulating the pointlight
var spherePointLight = new THREE.SphereGeometry(0.2);
var spherePointLightMaterial = new THREE.MeshBasicMaterial({color:});
var spherePointLightMesh = new THREE.Mesh(sphereLight,spherePointLightMaterial);
spherePointLightMesh.position = new THREE.vector3(3,0,5);
scene.add(spherePointLightMesh);

//add spotlight for the shadows
var spotLight = new THREE.spotLight(0xffffff, 1, 180,Math.PI / 4);
spotLight.shadow.mapSize.set(2048,2048);
spotLight.castShadow = true;
scene.add(apotLight);

//add spotlight for the shadows
var spotLight = new THREE.SpotLight(0xffffff, 1, 180, Math.PI / 4);
spotLight.shadow.mapSize.set(2048,2048);
spotLight.position.set(-30,40,-10);
spotLight.castShadow = true;
scene.add(spotLight);

